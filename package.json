{
  "name": "ageis2",
  "private": true,
  "version": "0.1.0",
  "description": "AEGIS AI — Governance-Native Game Development Agent (React/Hono monorepo)",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "build:frontend": "npm run build --workspace frontend",
    "deploy": "npm run build:frontend && cp -r frontend/dist backend/public && npm run deploy --workspace backend"
  }
}
